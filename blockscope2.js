if(false){
    var x=2
}
console.log(x); //undefined var is function scoped or globally scoped, so it can be accessed outside the block
//hoisting is a mechanism where variables and function declarations are moved to the top of their containing scope during the compile phase