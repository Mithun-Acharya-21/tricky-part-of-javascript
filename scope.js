function addTwenty(num){ //numR(ARG)
    var add=20;
    return num+add;    
}
/*console.log(add);//refrence error  (add is bound by the scope of the function here we are trying to access outside the function)

console.log(num);
let num=5; //refrence error

console.log(num);
var num=5;//undefined (declaration using let and const are evaluated at runtime but var are not)*/