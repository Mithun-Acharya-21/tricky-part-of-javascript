const twoSquared = (function squareNum(num) {
  return num * num;
}(2));
console.log(twoSquared); // 4, IIFE (Immediately Invoked Function Expression) is a function that is executed immediately after it is defined 